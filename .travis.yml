language: java

sudo: false

addons:
  apt:
    packages:
      - tcl
      - tcllib
      - rubygems

cache:
  - apt

before_script:
  - gem install --no-ri --no-rdoc cucumber

script:
  - gradle cucumber_tcl
